# application.properties -> server.port=8080
server:
  port: 8080

spring:
  application:
    name: 251204_sb
  profiles:
    active: dev
  # dev, prod 공통 설정
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

